{
  "name": "todo-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "portless run vp dev",
    "build": "vp build",
    "preview": "vp preview",
    "test": "pnpm --dir . exec vp test --mode test --no-file-parallelism",
    "deploy": "vp run build && wrangler deploy"
  },
  "dependencies": {
    "@do-sync-engine/core": "workspace:*",
    "@do-sync-engine/durable-object-websocket": "workspace:*",
    "@do-sync-engine/sql-regex-adapter": "workspace:*",
    "typed-rpc": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "latest",
    "@cloudflare/vitest-pool-workers": "latest",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@types/node": "^24.13.2",
    "portless": "catalog:",
    "svelte": "^5.56.3",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:",
    "wrangler": "latest"
  }
}
